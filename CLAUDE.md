# DataStream 项目开发规范

## 核心原则

**严格按照设计文档实施，不允许擅自简化或跳过。**

---

## 目录结构规范

Go项目必须遵循标准目录结构，区分可复用库与业务代码：

```
pkg/                          ← 可被外部项目导入（可复用库）
├── parser/                   ← 通用DDL解析库（设计良好可独立使用）
│   ├── mysql/
│   ├── postgres/
│   ├── oracle/
│   ├── sqlserver/
│   └── noop/
├── event/                    ← 事件模型（parser依赖，必须可导出）
├── config/                   ← 配置结构（可复用）
├── errors/                   ← 通用错误码（可复用）
├── logutil/                  ← 日志工具（可复用）
├── utils/                    ← 通用工具（可复用）
├── metrics/                  ← 监控指标（可复用）
└── version/                  ← 版本信息（可复用）

internal/                     ← 仅限本项目使用（业务代码）
├── source/                   ← Source连接器实现
├── sink/                     ← Sink连接器实现
├── pipeline/                 ← 管道核心逻辑
├── filter/                   ← 过滤模块
├── transform/                ← 转换模块
├── router/                   ← 路由模块
├── coordinator/              ← 协调器
├── offset/                   ← 位点存储
├── api/                      ← REST API
├── cli/                      ← CLI
└── app/                      ← 应用生命周期

cmd/                          ← 主程序入口
├── datastream/               ← 主服务
└── datastream-ctl/           ← 控制工具
```

### 判断标准

| 目录位置 | 判断标准 | 示例 |
|---------|---------|------|
| `pkg/` | 外部项目可能需要导入 | parser, utils, errors |
| `internal/` | 仅本项目使用 | source, sink, pipeline |
| `cmd/` | 可执行程序入口 | main.go |

### 重构原则

当发现代码位置不正确时：
1. **立即停止**当前开发
2. **移动代码**到正确位置
3. **更新import路径**
4. **验证编译和测试**

---

## 开发流程规范

### 1. 实施前检查

- [ ] 阅读相关设计文档（`docs/design/`）
- [ ] 确认设计文档中的技术选型、架构设计、接口定义
- [ ] 不理解的地方必须先澄清，不能自行假设

### 2. 实施过程

- [ ] 严格按照设计文档的方案实施
- [ ] 代码结构、文件命名、接口定义必须与设计文档一致
- [ ] 如果设计文档要求使用特定技术/库，必须使用

### 3. 完成后校验

- [ ] 对照设计文档逐项检查实现是否完整
- [ ] 功能、接口、数据结构必须与设计文档描述一致
- [ ] **不完整必须打回重新补充，不得以"简化版本"为由跳过**

---

## 阶段验收标准

每个开发阶段完成后，执行以下验收流程：

1. **文档对照检查**：列出设计文档要求 vs 实际实现
2. **差距分析**：明确标注任何偏差或缺失
3. **打回机制**：如有差距，必须补充完成，不得进入下一阶段

---

## 自动触发规则

### 需求讨论时

当用户提出新需求、讨论功能、规划任务时，**自动触发 brainstorming 流程**：

1. 明确需求范围
2. 查阅相关设计文档
3. 列出设计文档中的相关要求
4. 确认实施方案与设计文档一致
5. 如有偏差，必须先讨论并达成一致

---

## 文档优先级

```
设计文档 > 实施计划 > 代码实现
```

**任何代码实现必须能够追溯到设计文档中的定义。**

---

## 当前项目状态

- 设计文档目录：`docs/design/`
- 实施计划：`docs/design/implementation-plan.md`
- 项目进度：`MEMORY.md`

---

## 违规处理

如果发现实施与设计文档不符：

1. 立即停止当前工作
2. 分析差距原因
3. 按照 design document 重新实施
4. 更新 MEMORY.md 记录问题，避免再次发生

---

*创建时间：2026-05-09*
*版本：v1.0*
